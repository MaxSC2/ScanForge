{
  "identifier": "default",
  "description": "Default capability set for the main ScanForge window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
